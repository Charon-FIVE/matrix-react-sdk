import React from 'react';
import { DeviceWithVerification } from './types';
interface Props {
    isVerified?: DeviceWithVerification['isVerified'];
    isSelected?: boolean;
}
export declare const DeviceType: React.FC<Props>;
export {};
