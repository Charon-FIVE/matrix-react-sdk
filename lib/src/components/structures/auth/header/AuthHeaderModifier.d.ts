import { ReactNode } from "react";
interface Props {
    title: ReactNode;
    icon?: ReactNode;
    hideServerPicker?: boolean;
}
export declare function AuthHeaderModifier(props: Props): any;
export {};
